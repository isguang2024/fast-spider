package nodeui

const taskCenterHTML = `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="ui-token" content="{{UI_TOKEN}}">
  <title>任务中心 · Fast Spider Node</title>
  <style>
    :root{color-scheme:light;--bg:#f3f4f6;--panel:#fff;--panel-soft:#f8fafc;--line:#e2e6eb;--line-strong:#cbd2da;--text:#17202b;--muted:#65717d;--blue:#056dff;--blue-soft:#eaf2ff;--green:#157347;--green-soft:#eaf7ef;--amber:#a15c00;--amber-soft:#fff5df;--red:#b42318;--red-soft:#fff0ee;--purple:#6941c6;--purple-soft:#f1edff;--shadow:0 14px 40px rgba(23,32,43,.06)}
    *{box-sizing:border-box}html,body{min-height:100%;margin:0}body{background:var(--bg);color:var(--text);font:14px/1.55 ui-sans-serif,system-ui,-apple-system,"Segoe UI",sans-serif}button,input{font:inherit}button,a{color:inherit}button{cursor:pointer}button:focus-visible,a:focus-visible,input:focus-visible,summary:focus-visible{outline:2px solid var(--blue);outline-offset:2px}.app{width:min(1540px,calc(100% - 36px));margin:0 auto;padding:25px 0 48px}.topbar{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin-bottom:20px}.brand{display:flex;align-items:flex-start;gap:13px}.mark{width:42px;height:42px;border-radius:13px;background:#17202b;color:#fff;display:grid;place-items:center;font-weight:800;letter-spacing:-.04em;box-shadow:var(--shadow)}.eyebrow{margin:0 0 2px;color:var(--blue);font-size:11px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.brand h1{margin:0;font-size:25px;line-height:1.2;letter-spacing:-.025em}.subtitle{margin:4px 0 0;color:var(--muted)}.top-actions{display:flex;align-items:center;gap:8px;flex-wrap:wrap;justify-content:flex-end}.button{border:1px solid var(--line-strong);background:var(--panel);border-radius:9px;min-height:40px;padding:8px 12px}.button:hover{border-color:#9aa7b5;background:#fbfcfe}.button.primary{border-color:#17202b;background:#17202b;color:#fff}.button.compact{min-height:34px;padding:6px 10px;font-size:13px}.unread{display:none;min-width:21px;height:21px;padding:0 6px;border-radius:999px;background:var(--red);color:#fff;align-items:center;justify-content:center;font-size:12px}.unread.show{display:inline-flex}.status-line{display:flex;align-items:center;gap:7px;border:1px solid var(--line);border-radius:999px;background:var(--panel);padding:8px 11px;color:var(--muted);white-space:nowrap}.status-dot{width:8px;height:8px;border-radius:50%;background:#9aa4ad}.status-dot.online{background:var(--green)}.status-dot.offline{background:var(--red)}.status-dot.loading{background:var(--amber);animation:pulse 1.2s infinite}@keyframes pulse{50%{opacity:.35}}
    .notice{display:none;align-items:flex-start;gap:9px;border:1px solid #f0c1ba;border-radius:11px;background:var(--red-soft);color:var(--red);padding:11px 13px;margin-bottom:16px}.notice.show{display:flex}.notice strong{display:block}.notice span{color:#7d3933}.layout{display:grid;grid-template-columns:294px minmax(0,1fr);gap:18px;align-items:start}.side,.main-card{border:1px solid var(--line);border-radius:15px;background:var(--panel);box-shadow:var(--shadow)}.side{position:sticky;top:18px;overflow:hidden}.side-head{padding:16px 16px 13px;border-bottom:1px solid var(--line)}.side-head-row{display:flex;align-items:center;justify-content:space-between;gap:8px}.side h2{margin:0;font-size:15px}.side-count{font-size:12px;color:var(--muted)}.search{width:100%;margin-top:12px;min-height:39px;border:1px solid var(--line-strong);border-radius:9px;padding:8px 10px;background:#fff}.project-list{max-height:calc(100vh - 220px);overflow:auto;padding:9px}.controller-group{margin:0 0 8px}.controller-group>summary{list-style:none;cursor:pointer;border-radius:9px;padding:8px 9px;color:var(--muted);font-size:12px;font-weight:750}.controller-group>summary::-webkit-details-marker{display:none}.controller-group>summary:hover{background:var(--panel-soft)}.controller-label{display:flex;align-items:center;gap:7px}.controller-label:before{content:"⌄";font-size:14px;color:var(--muted)}.controller-group:not([open]) .controller-label:before{content:"›"}.controller-id{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.controller-projects{padding-left:5px}.project-button{display:block;width:100%;border:1px solid transparent;border-radius:11px;background:transparent;text-align:left;padding:11px 11px;margin:2px 0}.project-button:hover{background:var(--panel-soft)}.project-button.selected{border-color:#b9d3ff;background:var(--blue-soft)}.project-title{display:flex;align-items:flex-start;gap:8px}.project-title strong{display:block;min-width:0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1}.project-title .state-badge{flex:none}.project-root{display:block;color:var(--muted);font-size:12px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;margin-top:3px}.project-meta{display:flex;align-items:center;gap:8px;color:var(--muted);font-size:12px;margin-top:8px}.project-progress{height:5px;flex:1;border-radius:999px;background:#e9edf2;overflow:hidden}.project-progress i{display:block;height:100%;background:var(--green);border-radius:inherit}.empty{padding:22px 13px;color:var(--muted);text-align:center}.main-card{min-width:0}.main-head{display:flex;align-items:flex-start;justify-content:space-between;gap:15px;padding:19px 21px;border-bottom:1px solid var(--line)}.main-head h2{margin:0;font-size:20px;letter-spacing:-.02em}.main-head p{margin:4px 0 0;color:var(--muted)}.main-head-actions{display:flex;gap:8px;align-items:center;flex-wrap:wrap;justify-content:flex-end}.workspace{display:grid;grid-template-columns:minmax(260px,360px) minmax(0,1fr);min-height:650px}.tree-pane{border-right:1px solid var(--line);min-width:0}.tree-head{padding:14px 16px;border-bottom:1px solid var(--line);background:var(--panel-soft)}.tree-head-row{display:flex;align-items:center;justify-content:space-between;gap:8px}.tree-head strong{font-size:13px}.tree-summary{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.tree-scroll{max-height:calc(100vh - 300px);min-height:390px;overflow:auto;padding:9px}.tree-node{margin:1px 0}.tree-details{border-radius:9px}.tree-details[open]{background:#fbfcfe}.tree-details>summary{list-style:none;cursor:pointer;border-radius:9px;padding:8px 8px;display:flex;align-items:center;gap:7px}.tree-details>summary::-webkit-details-marker{display:none}.tree-details>summary:before{content:"›";color:var(--muted);width:13px;display:inline-block;font-size:19px;line-height:13px;transform:translateY(-1px)}.tree-details[open]>summary:before{transform:rotate(90deg) translateX(-1px)}.tree-details>summary:hover,.tree-leaf:hover{background:#f3f6fa}.tree-children{padding:0 0 3px 18px;border-left:1px solid var(--line);margin-left:15px}.tree-leaf{display:flex;align-items:flex-start;gap:7px;width:100%;border:0;background:transparent;border-radius:9px;text-align:left;padding:8px}.tree-leaf.selected{background:var(--blue-soft);color:#124b9d}.tree-icon{width:17px;height:17px;border-radius:5px;display:grid;place-items:center;flex:none;margin-top:1px;background:#e9edf2;color:#5d6975;font-size:10px;font-weight:800}.tree-icon.planner{background:var(--purple-soft);color:var(--purple)}.tree-copy{min-width:0;flex:1}.tree-title{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tree-sub{display:block;color:var(--muted);font-size:11px;margin-top:1px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.tree-details.planner{background:#fcfaff}.tree-details.planner>summary{color:#5732a9}.tree-details.group>summary{font-weight:700}.tree-kind{font-size:10px;padding:2px 5px;border-radius:5px;background:var(--purple-soft);color:var(--purple);font-weight:700;white-space:nowrap}.recovery-tag{font-size:10px;padding:2px 6px;border-radius:999px;background:var(--amber-soft);color:var(--amber);font-weight:750;white-space:nowrap}.recovery-tag.recovery-watching{background:var(--green-soft);color:var(--green)}.recovery-tag.recovery-uncertain{background:var(--red-soft);color:var(--red)}.detail-pane{min-width:0;padding:21px;overflow:hidden}.detail-empty{min-height:580px;display:grid;place-items:center;text-align:center;color:var(--muted);padding:40px}.detail-kicker{display:flex;align-items:center;gap:8px;flex-wrap:wrap;margin-bottom:8px}.detail-title{font-size:24px;line-height:1.25;letter-spacing:-.03em;margin:0;overflow-wrap:anywhere}.detail-subtitle{color:var(--muted);margin:7px 0 0;overflow-wrap:anywhere}.state-badge{display:inline-flex;align-items:center;border-radius:999px;padding:3px 8px;font-size:11px;font-weight:750;white-space:nowrap}.state-ok{color:var(--green);background:var(--green-soft)}.state-warn{color:var(--amber);background:var(--amber-soft)}.state-bad{color:var(--red);background:var(--red-soft)}.state-info{color:#1558a6;background:var(--blue-soft)}.state-planner{color:var(--purple);background:var(--purple-soft)}.state-muted{color:var(--muted);background:#eef1f4}.detail-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:9px;margin:19px 0}.metric{border:1px solid var(--line);border-radius:10px;padding:10px 11px;background:var(--panel-soft);min-width:0}.metric span{display:block;color:var(--muted);font-size:11px}.metric strong{display:block;margin-top:3px;overflow-wrap:anywhere}.detail-section{border-top:1px solid var(--line);padding-top:17px;margin-top:17px}.detail-section h3{font-size:13px;margin:0 0 9px}.prose{white-space:pre-wrap;overflow-wrap:anywhere;color:#303b47}.prose.muted{color:var(--muted)}.chips{display:flex;gap:6px;flex-wrap:wrap}.chip{border:1px solid var(--line);border-radius:999px;padding:4px 8px;color:var(--muted);font-size:12px;overflow-wrap:anywhere}.links{display:flex;gap:8px;flex-wrap:wrap}.links a{display:inline-flex;align-items:center;gap:5px;border:1px solid var(--line-strong);border-radius:8px;padding:7px 9px;color:#1558a6;text-decoration:none;font-size:12px}.links a:hover{background:var(--blue-soft);border-color:#b9d3ff}.check-list,.event-list{display:grid;gap:7px;margin:0;padding:0;list-style:none}.check-item,.event-item{border:1px solid var(--line);border-radius:9px;padding:9px 10px;background:var(--panel-soft)}.check-item{display:flex;justify-content:space-between;gap:12px}.check-item span:first-child{overflow-wrap:anywhere}.check-result{font-size:12px;color:var(--muted);text-align:right;overflow-wrap:anywhere}.event-item{display:grid;grid-template-columns:125px minmax(0,1fr);gap:10px}.event-time{color:var(--muted);font-size:12px}.event-summary{overflow-wrap:anywhere}.toast{position:fixed;right:20px;bottom:20px;width:min(390px,calc(100% - 40px));border:1px solid #c9d7ed;border-radius:12px;background:#f5f9ff;color:#174d93;padding:12px 14px;box-shadow:0 12px 30px rgba(23,32,43,.15);display:none;z-index:5}.toast.show{display:block}.toast strong{display:block}.toast span{display:block;color:#45627f;font-size:12px;margin-top:2px}.loading-state{padding:25px;color:var(--muted)}[hidden]{display:none!important}
    @media(max-width:960px){.layout{grid-template-columns:1fr}.side{position:static}.project-list{max-height:220px;display:flex;gap:7px;overflow:auto}.project-button{min-width:240px}.workspace{grid-template-columns:1fr}.tree-pane{border-right:0;border-bottom:1px solid var(--line)}.tree-scroll{max-height:390px;min-height:0}.detail-pane{padding:18px}.detail-empty{min-height:300px}}
    .planner-history{margin:10px 0;border:1px solid var(--line);border-radius:10px;background:var(--panel-soft)}.planner-history>summary{list-style:none;cursor:pointer;padding:9px;display:flex;align-items:center;gap:7px}.planner-history>summary::-webkit-details-marker{display:none}.planner-history-list{margin:0;padding:0 12px 10px 35px}.planner-history-item{padding:7px 0;color:var(--muted);cursor:pointer}.planner-history-item:hover{color:var(--text)}.planner-round{display:inline-block;min-width:54px;color:var(--purple);font-weight:750}.planner-summary{color:var(--text);overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.action-row{display:flex;gap:7px;flex-wrap:wrap;margin-top:12px}.button.danger{border-color:#e7b2ad;color:var(--red);background:var(--red-soft)}.button:disabled{cursor:wait;opacity:.6}
    @media(max-width:620px){.app{width:min(100% - 22px,1540px);padding-top:15px}.topbar,.main-head{flex-direction:column}.top-actions,.main-head-actions{width:100%;justify-content:flex-start}.status-line{order:-1}.brand h1{font-size:22px}.detail-grid{grid-template-columns:1fr 1fr}.event-item{grid-template-columns:1fr;gap:3px}.main-card{border-radius:12px}}
  </style>
</head>
<body>
  <main class="app">
    <header class="topbar">
      <div class="brand"><div class="mark">FS</div><div><p class="eyebrow">Fast Spider Node</p><h1>任务中心</h1><p class="subtitle">按主控分区查看任务进度、依赖和最近活动</p></div></div>
      <div class="top-actions"><div class="status-line"><i id="status-dot" class="status-dot loading"></i><span id="status-text">正在读取任务快照…</span></div><button id="enable-notifications" class="button compact" type="button">启用桌面通知</button><button id="clear-unread" class="button compact" type="button"><span id="unread-label">未读</span> <span id="unread-count" class="unread">0</span></button><a class="button compact" href="/">返回 Node</a><button id="refresh" class="button primary compact" type="button">刷新</button></div>
    </header>
    <div id="notice" class="notice" role="status"><strong>离线</strong><span id="notice-text">正在显示上次成功读取的快照。</span></div>
    <section class="layout">
      <aside class="side" aria-label="主控任务区导航">
        <div class="side-head"><div class="side-head-row"><h2>主控任务区</h2><span id="project-count" class="side-count">—</span></div><button id="toggle-archived" class="button compact" type="button" aria-pressed="false">显示已归档</button><input id="search" class="search" type="search" placeholder="搜索主控、项目或任务…" aria-label="搜索主控、项目或任务"></div>
        <div id="project-list" class="project-list"><div class="loading-state">正在加载任务区…</div></div>
      </aside>
      <section class="main-card" aria-live="polite">
        <div id="main-head" class="main-head"><div><h2>任务区</h2><p>选择一个主控任务区查看任务树</p></div><div id="main-head-actions" class="main-head-actions"></div></div>
        <div class="workspace">
          <section class="tree-pane" aria-label="任务树"><div id="tree-head" class="tree-head"><div class="tree-head-row"><strong>任务树</strong><button id="collapse-tree" class="button compact" type="button">折叠规划</button></div><div id="tree-summary" class="tree-summary"></div></div><div id="tree" class="tree-scroll"><div class="loading-state">正在加载任务树…</div></div></section>
          <section id="detail" class="detail-pane" aria-label="任务详情"><div class="detail-empty">选择左侧任务后查看目标、验收、依赖和活动。</div></section>
        </div>
      </section>
    </section>
    <div id="toast" class="toast" role="status" aria-live="polite"><strong id="toast-title"></strong><span id="toast-text"></span></div>
  </main>
<script>
(() => {
  const token = document.querySelector('meta[name="ui-token"]').content;
  const $ = id => document.getElementById(id);
  const state = {projects:[],selectedProjectId:'',selectedTaskId:'',taskDetail:null,events:[],eventCursor:0,eventCursors:{},expanded:new Set(),treeTouched:new Set(),controllerExpanded:new Set(),controllerTouched:new Set(),known:{},areaRequests:{},initialized:false,online:false,loading:false,error:'',areaErrors:{},eventsByProject:{},unread:0,desktopNotifications:false,toastTimer:0,showArchived:false,pendingAction:null,plannerHistoryOpen:false};

  async function api(path,options) {
    const request=Object.assign({},options || {}); request.headers=Object.assign({'X-Fast-Spider-UI-Token':token},request.headers || {});
    if(request.body && typeof request.body !== 'string'){request.headers['Content-Type']='application/json';request.body=JSON.stringify(request.body);}
    const response=await fetch(path,request);
    const data = await response.json().catch(() => ({}));
    if (!response.ok) throw new Error(data.error || ('HTTP ' + response.status));
    return data;
  }
  function make(tag,className,text) { const node=document.createElement(tag); if(className) node.className=className; if(text !== undefined && text !== null) node.textContent=String(text); return node; }
  function clear(node) { while(node.firstChild) node.removeChild(node.firstChild); }
  function value(value,fallback) { return value === undefined || value === null || String(value).trim() === '' ? (fallback || '—') : String(value); }
  function statusText(raw) {
    const key=String(raw || '').toLowerCase();
    return ({pending:'排队中',queued:'排队中',running:'执行中',in_progress:'执行中',working:'执行中',complete:'已完成',completed:'已完成',success:'已完成',failed:'失败',failure:'失败',error:'错误',blocked:'已阻塞',waiting:'等待中',needs_decision:'需要决定',paused:'已暂停',canceling:'取消中',cancelled:'已取消',canceled:'已取消',planned:'规划中',ready:'待开始',prepared:'已准备',active:'执行中',dispatched:'已派发',returned:'已返回待验收',accepted:'已验收',deferred:'已延后',checking:'检查中',rejected:'派发被拒绝'})[key] || value(raw,'未知');
  }
  function statusClass(raw) {
    const key=String(raw || '').toLowerCase();
    if(['complete','completed','success','accepted'].includes(key)) return 'state-ok';
    if(['failed','failure','error','blocked','rejected'].includes(key)) return 'state-bad';
    if(['running','in_progress','working','active','prepared','returned','checking','needs_decision','waiting','paused','deferred','canceling'].includes(key)) return 'state-warn';
    if(['planned','planner','plan'].includes(key)) return 'state-planner';
    if(['pending','queued','ready','dispatched'].includes(key)) return 'state-info';
    return 'state-muted';
  }
  function badge(raw) { return make('span','state-badge ' + statusClass(raw),statusText(raw)); }
  function timestamp(raw) {
    if(raw === undefined || raw === null || raw === '' || (typeof raw !== 'boolean' && Number(raw) === 0)) return '—';
    const numeric=Number(raw); const date=Number.isFinite(numeric) && numeric > 100000000 ? new Date(numeric * 1000) : new Date(String(raw));
    if(Number.isNaN(date.getTime())) return String(raw);
    return date.toLocaleString('zh-CN',{year:'numeric',month:'2-digit',day:'2-digit',hour:'2-digit',minute:'2-digit'});
  }
  function shortTime(raw) { return timestamp(raw); }
  function taskKey(projectId,taskId) { return String(projectId) + ':' + String(taskId); }
  function currentProject() { return state.projects.find(item => String(item.project && item.project.id) === String(state.selectedProjectId)) || null; }
  function currentTasks() { const item=currentProject(); return item && Array.isArray(item.tasks) ? item.tasks.filter(task => state.showArchived || !task.archived) : []; }
  function currentTask() { return currentTasks().find(item => String(item.id) === String(state.selectedTaskId)) || null; }
  function selectedEvents() { return Array.isArray(state.eventsByProject[state.selectedProjectId]) ? state.eventsByProject[state.selectedProjectId] : []; }
  function normalizeProject(item) {
    const project=item && item.project ? item.project : item || {};
    return {project:project,tasks:Array.isArray(item && item.tasks) ? item.tasks : [],groups:(item && item.groups) || {},complete:!!(item && item.complete),pendingAcknowledgements:Number(item && item.pendingAcknowledgements || 0),cooldownUntil:Number(item && item.cooldownUntil || 0),updatedAt:item && item.updatedAt};
  }
  function projectTitle(item) { return value(item && item.project && (item.project.title || item.project.goalSummary),'未命名任务区'); }
  function projectState(item) {
    const p=item && item.project || {}; if(item && item.complete === true) return 'complete'; if(p.state) return String(p.state); if(p.paused) return 'paused';
    const tasks=item && Array.isArray(item.tasks) ? item.tasks : [];
    if(tasks.some(task => ['failed','failure','error','blocked','rejected'].includes(String(task.state || '').toLowerCase()))) return 'failed';
    if(tasks.some(task => ['active','prepared','running','in_progress','working'].includes(String(task.state || '').toLowerCase()))) return 'running';
    if(tasks.some(task => ['returned','checking'].includes(String(task.state || '').toLowerCase()))) return 'checking';
    return tasks.length ? 'pending' : 'planned';
  }
  function progress(item) {
    const tasks=item && Array.isArray(item.tasks) ? item.tasks.filter(task => !isPlanner(task) && !['canceling','cancelled','canceled'].includes(String(task.state || '').toLowerCase())) : []; const done=tasks.filter(task => ['accepted','complete','completed','success'].includes(String(task.state || '').toLowerCase())).length;
    return {done:done,total:tasks.length,percent:tasks.length ? Math.round(done * 100 / tasks.length) : 0};
  }
  function taskSignature(task) { return [task.state,task.reason,task.waitingReason,Array.isArray(task.blockedBy) ? task.blockedBy.join(',') : '',task.error].map(value => String(value || '')).join('|'); }
  function needsNotification(task) {
    const key=String(task.state || '').toLowerCase(); const waiting=String(task.waitingReason || task.reason || '').toLowerCase();
    return ['complete','completed','success','failed','failure','error','blocked','needs_decision'].includes(key) || waiting.includes('decision') || waiting.includes('approve') || waiting.includes('input') || waiting.includes('clarif');
  }
  function observeTasks(projectId,tasks) {
    (Array.isArray(tasks) ? tasks : []).forEach(task => {
      const key=taskKey(projectId,task.id); const signature=taskSignature(task); const previous=state.known[key];
      if(state.initialized && previous && previous !== signature && needsNotification(task)) {
        const project=state.projects.find(item => String(item.project && item.project.id) === String(projectId));
        addNotification(project,task);
      }
      state.known[key]=signature;
    });
  }
  function addNotification(project,task) {
    state.unread += 1; updateUnread();
    const title=projectTitle(project) + ' · ' + value(task.title,'任务状态变化'); const text=statusText(task.state) + (task.reason || task.waitingReason ? ' · ' + value(task.reason || task.waitingReason,'') : ''); showToast(title,text);
    if(state.desktopNotifications && 'Notification' in window && Notification.permission === 'granted') new Notification(title,{body:text});
  }
  function updateUnread() { $('unread-count').textContent=String(state.unread); $('unread-count').classList.toggle('show',state.unread > 0); $('unread-label').textContent=state.unread > 0 ? '未读' : '无未读'; }
  function showToast(title,text) { $('toast-title').textContent=title; $('toast-text').textContent=text; $('toast').classList.add('show'); clearTimeout(state.toastTimer); state.toastTimer=setTimeout(() => $('toast').classList.remove('show'),6500); }
  function renderHeader() {
    const dot=$('status-dot'); dot.className='status-dot ' + (state.loading ? 'loading' : state.online ? 'online' : 'offline'); $('status-text').textContent=state.loading ? '正在同步任务快照…' : state.online ? '已同步' : '离线 · 使用旧快照';
    const visibleProjects=state.projects.filter(item => state.showArchived || !(item.project || {}).archived); $('project-count').textContent=visibleProjects.length ? visibleProjects.length + ' 个区' : '暂无任务区'; const archiveButton=$('toggle-archived'); archiveButton.textContent=state.showArchived ? '隐藏已归档' : '显示已归档'; archiveButton.setAttribute('aria-pressed',String(state.showArchived)); updateUnread();
    const notice=$('notice'); notice.classList.toggle('show',!!state.error); $('notice-text').textContent=state.error ? state.error + (state.projects.length ? ' · 当前仍保留上次成功快照。' : '') : '';
  }
  function renderProjectList() {
    const list=$('project-list'); clear(list); const query=$('search').value.trim().toLowerCase();
    const visible=state.projects.filter(item => { const p=item.project || {}; if(!state.showArchived && p.archived) return false; if(!query) return true; const hay=[p.title,p.root,p.goalSummary,p.id,p.controllerSessionId].concat((item.tasks || []).filter(task => state.showArchived || !task.archived).map(task => task.title)).join(' ').toLowerCase(); return hay.includes(query); });
    if(!visible.length){list.appendChild(make('div','empty',state.projects.length ? '没有匹配的主控或任务。' : '暂无主控任务区。'));return;}
    const grouped=new Map(); visible.forEach(item => { const controller=String(item.project && item.project.controllerSessionId || '未绑定主控'); if(!grouped.has(controller)) grouped.set(controller,[]); grouped.get(controller).push(item); });
    grouped.forEach((items,controller) => { const group=make('details','controller-group'); const key='controller:' + controller; group.open=state.controllerTouched.has(key) ? state.controllerExpanded.has(key) : true; const summary=document.createElement('summary'); const label=make('span','controller-label'); label.appendChild(make('span','controller-id',controller === '未绑定主控' ? controller : '主控 ' + controller)); summary.appendChild(label); group.addEventListener('toggle',() => { state.controllerTouched.add(key); if(group.open) state.controllerExpanded.add(key); else state.controllerExpanded.delete(key); }); const projects=make('div','controller-projects'); items.forEach(item => { const p=item.project || {}, button=make('button','project-button' + (String(p.id) === String(state.selectedProjectId) ? ' selected' : '')); button.type='button'; button.dataset.projectId=String(p.id || ''); button.setAttribute('aria-pressed',String(p.id) === String(state.selectedProjectId) ? 'true' : 'false'); const title=make('div','project-title'); title.append(make('strong',null,projectTitle(item)),badge(projectState(item))); button.appendChild(title); button.appendChild(make('span','project-root',value(p.root,'未提供项目路径'))); const meta=make('div','project-meta'), stats=progress(item), track=make('span','project-progress'), fill=make('i'); fill.style.width=stats.percent + '%'; track.appendChild(fill); meta.append(track,make('span',null,stats.done + '/' + stats.total)); if(p.archived) meta.appendChild(make('span','state-badge state-muted','已归档')); if(p.notification && p.notification.pending) meta.appendChild(make('span','state-badge state-warn','待处理')); button.appendChild(meta); button.addEventListener('click',() => selectProject(String(p.id || ''))); projects.appendChild(button); }); group.append(summary,projects); list.appendChild(group); });
  }
  function groupRecords() { return []; }
  function isPlanner(task) { const kind=String(task && task.kind || '').toLowerCase(); return kind.includes('plan') || kind.includes('planner') || kind.includes('coord') || kind.includes('orches'); }
  function executionTasks(item) { return (item && Array.isArray(item.tasks) ? item.tasks : []).filter(task => !isPlanner(task) && (state.showArchived || !task.archived)); }
  function plannerTasks(item) { return (item && Array.isArray(item.tasks) ? item.tasks : []).filter(task => isPlanner(task) && (state.showArchived || !task.archived)); }
  function kindText(raw) { return ({work:'执行',planner:'规划',plan:'规划',coordination:'协调',orchestrator:'协调'})[String(raw || '').toLowerCase()] || value(raw,'任务'); }
  function recoveryPhaseText(raw) { return ({watching:'观察中',waiting_job:'等待作业',probing:'核查中',continuing:'续推中',handover:'上下文接力',uncertain:'状态不确定'})[String(raw || '').toLowerCase()] || value(raw,'恢复处理中'); }
  function isTerminalTask(task) { return ['accepted','complete','completed','success','cancelled','canceled'].includes(String(task && task.state || '').toLowerCase()); }
  function recoveryTag(task) { const recovery=task && task.recovery; if(isTerminalTask(task) || !recovery || !recovery.phase) return null; const phase=String(recovery.phase).toLowerCase().replace(/[^a-z0-9_-]/g,''); const tag=make('span','recovery-tag recovery-' + phase,recoveryPhaseText(recovery.phase)); tag.title='恢复状态：' + recoveryPhaseText(recovery.phase); return tag; }
  function nodeSummary(node,group) { const title=group ? node.title : value(node.title,'未命名任务'); const summary=make('div','tree-copy'); summary.appendChild(make('span','tree-title',title)); if(!group && (node.kind || node.state)) summary.appendChild(make('span','tree-sub',kindText(node.kind) + ' · ' + statusText(node.state))); return summary; }
  function taskChildren(tasks,parent) { return tasks.filter(task => String(task.parent || '') === String(parent || '')); }
  function shortResultSummary(task) { const result=task && task.result; if(result && typeof result === 'object') return value(result.summary || result.shortSummary || result.outcome,'暂无规划结果'); return value(task && (task.summary || result),'暂无规划结果'); }
  function plannerRecords(item) { const records=[]; if(item && Array.isArray(item.plannerHistory)) item.plannerHistory.forEach(record => records.push(record)); plannerTasks(item).forEach(task => { if(Array.isArray(task.history) && task.history.length) task.history.forEach(record => records.push(Object.assign({},record,{taskId:task.id,title:task.title}))); else records.push(task); }); return records.sort((a,b) => Number(a.round || 0) - Number(b.round || 0)); }
  function projectTerminal(item) { const p=item && item.project || {}; return item && item.complete === true || ['cancelled','canceled'].includes(String(p.state || '').toLowerCase()); }
  function actionButton(label,action,projectId,taskId,danger) { const button=make('button','button compact' + (danger ? ' danger' : ''),label); button.type='button'; button.disabled=!!state.pendingAction; button.addEventListener('click',() => performAction(projectId,taskId,action)); return button; }
  function renderActionButtons(item,task,includeProject) { const row=make('div','action-row'); const project=item && item.project || {}; const projectID=String(project.id || ''); if(task){const taskID=String(task.id || ''); const stateKey=String(task.state || '').toLowerCase(); if(!['accepted','complete','completed','success','cancelled','canceled','canceling'].includes(stateKey) && !task.archived) row.appendChild(actionButton('取消任务','cancel',projectID,taskID,true)); if(task.archived) row.appendChild(actionButton('取消归档','unarchive',projectID,taskID,false)); else if(isTerminalTask(task)) row.appendChild(actionButton('归档任务','archive',projectID,taskID,false));} if(includeProject && project.archived) row.appendChild(actionButton('取消归档项目','unarchive',projectID,'',false)); else if(includeProject){if(!projectTerminal(item) && !['cancelled','canceled','canceling'].includes(String(project.state || '').toLowerCase())) row.appendChild(actionButton('取消项目','cancel',projectID,'',true)); if(projectTerminal(item)) row.appendChild(actionButton('归档项目','archive',projectID,'',false));} return row; }
  async function performAction(projectId,taskId,action) { const pinnedProject=String(projectId || ''), pinnedTask=String(taskId || ''); if(!pinnedProject || state.pendingAction) return; if(action === 'cancel' && !window.confirm(taskId ? '确认取消这个任务？已写入的文件会保留，正在执行的停止可能需要一段时间。' : '确认取消这个项目？已写入的文件会保留，正在执行的停止可能需要一段时间。')) return; state.pendingAction={projectId:pinnedProject,taskId:pinnedTask,action:action}; state.selectedProjectId=pinnedProject; state.selectedTaskId=pinnedTask; renderAll(); try { const body={action:action}; if(pinnedTask) body.taskId=pinnedTask; if(action === 'cancel') body.evidence='用户从任务中心请求取消'; await api('/api/tasks/' + encodeURIComponent(pinnedProject) + '/actions',{method:'POST',body:body}); showToast('请求已受理',action === 'cancel' ? '状态正在更新，文件会保留。' : action === 'archive' ? '归档请求已受理。' : '取消归档请求已受理，不会恢复执行。'); await refreshAll(); } catch(error) { state.error='任务操作失败：' + error.message; showToast('任务操作失败',error.message); } finally { state.pendingAction=null; renderAll(); } }
  function renderTreeNode(node,children,level) {
    const group=node.type === 'group', planning=group || isPlanner(node), hasChildren=children.length > 0, key='tree:' + state.selectedProjectId + ':' + node.id;
    const wrap=make('div','tree-node');
    if(hasChildren || planning){ const details=make('details','tree-details' + (group ? ' group' : '') + (planning && !group ? ' planner' : '')); details.open=state.treeTouched.has(key) ? state.expanded.has(key) : (!planning && level < 1); const summary=document.createElement('summary'); summary.appendChild(make('span','tree-icon' + (planning ? ' planner' : ''),group ? '组' : planning ? '规划' : '执行')); summary.appendChild(nodeSummary(node,group)); if(planning) summary.appendChild(make('span','tree-kind',group ? '规划组' : '规划')); if(!group && node.state) summary.appendChild(badge(node.state)); const recovery=recoveryTag(node); if(recovery) summary.appendChild(recovery); summary.addEventListener('click',event => { event.preventDefault(); state.treeTouched.add(key); if(details.open) state.expanded.delete(key); else state.expanded.add(key); if(!group) selectTask(state.selectedProjectId,String(node.id)); else renderTree(); }); const childrenBox=make('div','tree-children'); children.forEach(child => childrenBox.appendChild(renderTreeNode(child,taskChildren(currentTasks(),child.id),level + 1))); details.append(summary,childrenBox); wrap.appendChild(details);
    } else { const button=make('button','tree-leaf' + (String(node.id) === String(state.selectedTaskId) ? ' selected' : '')); button.type='button'; button.dataset.taskId=String(node.id || ''); button.appendChild(make('span','tree-icon',isPlanner(node) ? '规划' : '执行')); button.appendChild(nodeSummary(node,false)); if(node.state) button.appendChild(badge(node.state)); const recovery=recoveryTag(node); if(recovery) button.appendChild(recovery); button.addEventListener('click',() => selectTask(state.selectedProjectId,String(node.id))); wrap.appendChild(button); }
    return wrap;
  }
  function renderTree() {
    const tree=$('tree'), oldScroll=tree.scrollTop; clear(tree); const item=currentProject(); if(!item){tree.appendChild(make('div','empty','请选择一个主控任务区。'));return;}
    const tasks=executionTasks(item), groups=groupRecords(item), groupIds=new Set(groups.map(group => group.id)), taskIds=new Set(tasks.map(task => String(task.id))), rootTasks=tasks.filter(task => { const parent=String(task.parent || ''); return !parent || (!taskIds.has(parent) && !groupIds.has(parent)); });
    if(!tasks.length && !groups.length && !plannerTasks(item).length){tree.appendChild(make('div','empty','这个任务区还没有可展示的任务。'));tree.scrollTop=oldScroll;return;}
    groups.forEach(group => { const children=tasks.filter(task => String(task.parent || '') === group.id); tree.appendChild(renderTreeNode(Object.assign({},group,{type:'group'}),children,0)); });
    rootTasks.forEach(task => tree.appendChild(renderTreeNode(task,taskChildren(tasks,task.id),0))); const planners=plannerRecords(item); if(planners.length){const history=make('details','planner-history'); history.open=state.plannerHistoryOpen; history.addEventListener('toggle',() => {state.plannerHistoryOpen=history.open;}); const summary=document.createElement('summary'); summary.append(make('span','tree-icon planner','规划'),make('strong',null,'规划历史'),make('span','tree-kind',planners.length + ' 轮')); history.appendChild(summary); const list=make('ol','planner-history-list'); planners.forEach(record => {const row=make('li','planner-history-item'); row.append(make('span','planner-round','第 ' + value(record.round,'—') + ' 轮'),make('span','planner-summary',shortResultSummary(record))); row.addEventListener('click',() => selectTask(state.selectedProjectId,String(record.taskId || record.id || ''))); list.appendChild(row);}); history.appendChild(list); tree.appendChild(history);} tree.scrollTop=oldScroll;
  }
  function renderTreeHead() { const item=currentProject(); const summary=$('tree-summary'); clear(summary); if(!item){$('tree-head').hidden=true;return;} $('tree-head').hidden=false; const stats=progress(item), p=item.project || {}; summary.append(badge(projectState(item)),make('span','chip',stats.done + '/' + stats.total + ' 已完成')); if(p.revision || p.plannedRevision) summary.appendChild(make('span','chip','版本 ' + value(p.revision,p.plannedRevision))); if(p.pendingChanges) summary.appendChild(make('span','chip','待处理变更 ' + (Array.isArray(p.pendingChanges) ? p.pendingChanges.length : value(p.pendingChanges,'有')))); if(item.pendingAcknowledgements) summary.appendChild(make('span','chip',item.pendingAcknowledgements + ' 项待确认')); if(item.cooldownUntil > Math.floor(Date.now() / 1000)) summary.appendChild(make('span','chip','冷却至 ' + timestamp(item.cooldownUntil))); }
  function section(title,content) { const box=make('section','detail-section'); box.appendChild(make('h3',null,title)); if(content) box.appendChild(content); return box; }
  function textBlock(raw,emptyLabel) { return make('div','prose' + (raw ? '' : ' muted'),value(raw,emptyLabel || '暂无')); }
  function metric(label,content) { const box=make('div','metric'); box.append(make('span',null,label),make('strong',null,content)); return box; }
  function appendSessionLinks(parent,controllerSessionId,receiptSessionId) { const links=make('div','links'); if(controllerSessionId){ const codex=make('a',null,'打开 Codex 主控'); codex.href='codex://threads/' + encodeURIComponent(String(controllerSessionId)); codex.target='_blank'; codex.rel='noreferrer'; links.appendChild(codex); } if(receiptSessionId){ const chat=make('a',null,'打开 CHAT'); chat.href='https://chatgpt.com/c/' + encodeURIComponent(String(receiptSessionId)); chat.target='_blank'; chat.rel='noreferrer'; links.appendChild(chat); } if(links.childNodes.length) parent.appendChild(links); }
  function renderChecks(raw) { const list=make('ul','check-list'); if(!raw || (Array.isArray(raw) && !raw.length) || (!Array.isArray(raw) && typeof raw === 'object' && !Object.keys(raw).length)){list.appendChild(make('li','check-item','暂无检查结果'));return list;} if(Array.isArray(raw)){raw.forEach((item,index) => { const row=make('li','check-item'); row.append(make('span',null,value(item && (item.name || item.title || item.check),'检查 ' + (index + 1))),make('span','check-result',value(item && (item.result || item.status || item.state),typeof item === 'string' ? item : '—'))); list.appendChild(row); }); } else { Object.keys(raw).forEach(key => { const row=make('li','check-item'); row.append(make('span',null,key),make('span','check-result',typeof raw[key] === 'object' ? JSON.stringify(raw[key]) : value(raw[key]))); list.appendChild(row); }); } return list; }
  function renderRecovery(task) { const recovery=task && task.recovery; if(isTerminalTask(task) || !recovery || !recovery.phase) return null; const box=make('div'); const metrics=make('div','detail-grid'); metrics.append(metric('恢复阶段',recoveryPhaseText(recovery.phase)),metric('最后有效进展',timestamp(recovery.lastProgressAt)),metric('下次核查',timestamp(recovery.nextProbeAt)),metric('连续无进展重试',value(recovery.attempts,0)),metric('会话代次',value(recovery.sessionGeneration || task.round,'—'))); box.appendChild(metrics); const checkpoint=recovery.checkpoint || {}; if(checkpoint.summary) box.appendChild(section('最后有效进展摘要',textBlock(checkpoint.summary))); if(checkpoint.nextStep) box.appendChild(section('下一步',textBlock(checkpoint.nextStep))); if(checkpoint.stage) box.appendChild(section('接力阶段',textBlock(checkpoint.stage))); if(Array.isArray(checkpoint.evidence) && checkpoint.evidence.length) { const evidence=make('ul','check-list'); checkpoint.evidence.forEach(item => evidence.appendChild(make('li','check-item',item))); box.appendChild(section('接力证据',evidence)); } if(Array.isArray(checkpoint.waitingJobs) && checkpoint.waitingJobs.length) { const jobs=make('div','chips'); checkpoint.waitingJobs.forEach(item => jobs.appendChild(make('span','chip',item))); box.appendChild(section('等待中的作业',jobs)); } if(recovery.lastError) box.appendChild(section('最近恢复错误',textBlock(recovery.lastError))); return box; }
  function renderEvents(events) { const list=make('ul','event-list'); if(!events.length){list.appendChild(make('li','event-item','暂无最近活动'));return list;} events.slice().sort((a,b) => Number(b.id || 0) - Number(a.id || 0)).slice(0,30).forEach(event => { const row=make('li','event-item'); row.append(make('span','event-time',shortTime(event.at)),make('span','event-summary',value(event.summary,event.kind || '事件'))); list.appendChild(row); }); return list; }
  function renderProjectDetail(item) { const detail=$('detail'); clear(detail); const p=item.project || {}, stats=progress(item); detail.append(make('div','detail-kicker',null)); const kicker=detail.firstChild; kicker.append(badge(projectState(item)),make('span','chip',stats.done + '/' + stats.total + ' 已完成')); if(p.paused) kicker.appendChild(make('span','chip','主控已暂停')); detail.appendChild(make('h2','detail-title',projectTitle(item))); detail.appendChild(make('p','detail-subtitle',value(p.root,'未提供项目路径'))); const metrics=make('div','detail-grid'); metrics.append(metric('任务总数',stats.total),metric('待确认',value(item.pendingAcknowledgements,0)),metric('目标状态',item.complete ? '后端已确认完成' : '尚未完成')); detail.appendChild(metrics); detail.appendChild(section('主控目标',textBlock(p.goal || p.goalSummary,'暂无目标摘要'))); if(Array.isArray(p.questions) && p.questions.length){ const questions=make('ul','check-list'); p.questions.forEach(question => questions.appendChild(make('li','check-item',question))); detail.appendChild(section('待主控决定',questions)); } if(p.notification && p.notification.pending) detail.appendChild(section('待处理通知',textBlock(p.notification.error || p.notification.summary,'页面打开后请查看主控通知'))); appendSessionLinks(detail,p.controllerSessionId); detail.appendChild(section('最近活动',renderEvents(selectedEvents()))); }
  function renderTaskDetail(item,task) { const detail=$('detail'); clear(detail); const brief=task || {}; const full=Object.assign({},brief,(state.taskDetail && state.taskDetail.task) || {}); const p=(state.taskDetail && state.taskDetail.project) || item.project || {}; const kicker=make('div','detail-kicker'); kicker.append(badge(full.state),make('span','chip',kindText(full.kind))); if(full.round !== undefined && full.round !== null) kicker.appendChild(make('span','chip','第 ' + full.round + ' 轮')); detail.appendChild(kicker); detail.appendChild(renderActionButtons(item,full,false)); detail.appendChild(make('h2','detail-title',value(full.title,'未命名任务'))); detail.appendChild(make('p','detail-subtitle',value(full.objective || full.scope || full.reason,'暂无任务摘要'))); const metrics=make('div','detail-grid'); metrics.append(metric('状态',statusText(full.state)),metric('更新时间',timestamp(full.updatedAt || task && task.updatedAt)),metric('下一时间点',timestamp(full.nextAt || full.resumeAt || task && (task.nextAt || task.resumeAt)))); detail.appendChild(metrics); const recoveryView=renderRecovery(full); if(recoveryView) detail.appendChild(section('恢复状态',recoveryView)); if(full.objective) detail.appendChild(section('目标',textBlock(full.objective))); if(full.acceptance) detail.appendChild(section('验收标准',textBlock(full.acceptance))); if(full.context) detail.appendChild(section('上下文',textBlock(full.context))); const reason=full.waitingReason || full.reason || full.lastError || full.error; if(reason) detail.appendChild(section('依赖 / 排队原因',textBlock(reason))); if(Array.isArray(full.blockedBy) && full.blockedBy.length) { const chips=make('div','chips'); full.blockedBy.forEach(id => chips.appendChild(make('span','chip','依赖 ' + id))); detail.appendChild(section('阻塞依赖',chips)); } if(full.scope) detail.appendChild(section('范围',textBlock(full.scope))); if(full.validations || full.checks) detail.appendChild(section('检查结果',renderChecks(full.validations || full.checks))); if(full.result) detail.appendChild(section('结果',textBlock(typeof full.result === 'string' ? full.result : JSON.stringify(full.result)))); appendSessionLinks(detail,p.controllerSessionId,full.sessionId || (full.receipt && full.receipt.sessionId) || (task && task.sessionId)); detail.appendChild(section('最近活动',renderEvents(selectedEvents().filter(event => !event.taskId || String(event.taskId) === String(full.id))))); }
  function renderDetails() { const item=currentProject(); const head=$('main-head').firstElementChild; const actions=$('main-head-actions'); clear(actions); if(!item){head.querySelector('h2').textContent='任务区';head.querySelector('p').textContent='选择一个主控任务区查看任务树'; const detail=$('detail'); clear(detail); detail.appendChild(make('div','detail-empty','选择左侧任务后查看目标、验收、依赖和活动。')); return;} const p=item.project || {}; head.querySelector('h2').textContent=projectTitle(item); head.querySelector('p').textContent=value(p.goalSummary || p.root,'当前主控任务区'); actions.appendChild(renderActionButtons(item,null,true)); const selectedTask=state.selectedTaskId ? currentTask() : null; if(selectedTask){renderTaskDetail(item,selectedTask);} else {state.selectedTaskId=''; renderProjectDetail(item);} }
  function renderAll() { renderHeader(); renderProjectList(); renderTreeHead(); renderTree(); renderDetails(); }
  function mergeArea(data) { const item=normalizeProject(data); const id=String(item.project && item.project.id || ''); if(!id) return; const index=state.projects.findIndex(existing => String(existing.project && existing.project.id) === id); if(index >= 0){ const previous=state.projects[index]; item.project=Object.assign({},previous.project,item.project); state.projects[index]=Object.assign({},previous,item); } else state.projects.push(item); observeTasks(id,item.tasks); if(String(id) === String(state.selectedProjectId) && state.selectedTaskId && !item.tasks.some(task => String(task.id) === String(state.selectedTaskId))){state.selectedTaskId='';state.taskDetail=null;} }
  function mergeSnapshot(data) { if(!data || !Array.isArray(data.projects)) throw new Error('任务快照格式无效'); data.projects.forEach(item => { const incoming=normalizeProject(item); const id=String(incoming.project && incoming.project.id || ''); if(!id) return; const index=state.projects.findIndex(existing => String(existing.project && existing.project.id) === id); if(index >= 0){const previous=state.projects[index]; incoming.project=Object.assign({},previous.project,incoming.project); state.projects[index]=Object.assign({},previous,incoming);} else state.projects.push(incoming); observeTasks(id,incoming.tasks); }); const incomingIds=new Set(data.projects.map(item => String((item.project || {}).id || ''))); state.projects=state.projects.filter(item => incomingIds.has(String(item.project && item.project.id))); if(!state.selectedProjectId || !state.projects.some(item => String(item.project && item.project.id) === String(state.selectedProjectId))) state.selectedProjectId=state.projects[0] ? String(state.projects[0].project.id) : ''; if(!state.selectedProjectId) state.selectedTaskId=''; const selected=currentProject(); if(state.selectedTaskId && (!selected || !selected.tasks.some(task => String(task.id) === String(state.selectedTaskId)))){state.selectedTaskId='';state.taskDetail=null;} }
  async function loadArea(projectId) { const requestId=(state.areaRequests[projectId] || 0) + 1; state.areaRequests[projectId]=requestId; try { const data=await api('/api/tasks/' + encodeURIComponent(String(projectId))); mergeArea(data); const cursor=Number(state.eventCursors[projectId] || 0); const eventsData=await api('/api/tasks/' + encodeURIComponent(String(projectId)) + '/events?after=' + encodeURIComponent(String(cursor))); const incoming=Array.isArray(eventsData.events) ? eventsData.events : []; const existing=Array.isArray(state.eventsByProject[projectId]) ? state.eventsByProject[projectId] : []; const knownIds=new Set(existing.map(event => String(event.id))); incoming.forEach(event => { if(!knownIds.has(String(event.id))) existing.push(event); }); state.eventsByProject[projectId]=existing.slice(-100); state.eventCursors[projectId]=Number(eventsData.cursor || cursor); if(requestId !== state.areaRequests[projectId]) return; delete state.areaErrors[projectId]; if(String(projectId) === String(state.selectedProjectId)){state.error='';renderAll(); if(state.selectedTaskId) loadTaskDetail(projectId,state.selectedTaskId);} } catch(error) { if(requestId !== state.areaRequests[projectId]) return; state.areaErrors[projectId]=error.message; if(String(projectId) === String(state.selectedProjectId)){state.error='任务区读取失败：' + error.message;renderAll();} } }
  async function loadTaskDetail(projectId,taskId) { try { const data=await api('/api/tasks/' + encodeURIComponent(String(projectId)) + '/tasks/' + encodeURIComponent(String(taskId))); if(String(projectId) !== String(state.selectedProjectId) || String(taskId) !== String(state.selectedTaskId)) return; state.taskDetail=data; renderDetails(); } catch(error) { if(String(projectId) === String(state.selectedProjectId) && String(taskId) === String(state.selectedTaskId)){state.taskDetail=null;state.error='任务详情读取失败：' + error.message;renderAll();} } }
  async function refreshAll() { if(state.loading || document.hidden) return; state.loading=true; renderHeader(); try { const data=await api('/api/tasks'); mergeSnapshot(data); state.online=true; state.error=''; state.initialized=true; renderAll(); if(state.selectedProjectId) await loadArea(state.selectedProjectId); } catch(error) { state.online=false; state.error='任务快照读取失败：' + error.message; renderAll(); } finally { state.loading=false; renderHeader(); } }
  function selectProject(id) { if(state.pendingAction && String(id) !== String(state.pendingAction.projectId)) return; if(String(id) === String(state.selectedProjectId)) return; state.selectedProjectId=String(id); state.selectedTaskId=''; state.taskDetail=null; state.eventCursor=Number(state.eventCursors[id] || 0); state.error=''; renderAll(); loadArea(id); }
  function selectTask(projectId,taskId) { if(state.pendingAction && (String(projectId) !== String(state.pendingAction.projectId) || String(taskId) !== String(state.pendingAction.taskId))) return; state.selectedProjectId=String(projectId); state.selectedTaskId=String(taskId); state.taskDetail=null; renderAll(); loadTaskDetail(projectId,taskId); }
  function collapsePlanning() { state.expanded.forEach(key => { if(key.indexOf('tree:' + state.selectedProjectId + ':') === 0) { const id=key.split(':').slice(2).join(':'); const task=currentTasks().find(item => String(item.id) === id); const group=groupRecords(currentProject()).find(item => String(item.id) === id); if(group || isPlanner(task)){state.expanded.delete(key);state.treeTouched.add(key);} } }); renderTree(); }
  $('search').addEventListener('input',renderProjectList); $('refresh').addEventListener('click',refreshAll); $('clear-unread').addEventListener('click',() => {state.unread=0;updateUnread();}); $('collapse-tree').addEventListener('click',collapsePlanning); $('toggle-archived').addEventListener('click',() => {state.showArchived=!state.showArchived; renderAll();});
  $('enable-notifications').addEventListener('click',async () => { if(!('Notification' in window)){showToast('浏览器不支持桌面通知','页面内通知仍会正常显示。');return;} if(Notification.permission === 'default') await Notification.requestPermission(); if(Notification.permission === 'granted'){state.desktopNotifications=true;showToast('桌面通知已启用','页面打开时，完成、失败或需要决定的状态变化会提醒你；页面隐藏时暂停采集。');} else showToast('桌面通知未启用','可在浏览器设置中允许本机页面通知。'); });
  document.addEventListener('keydown',event => { if(event.key === '/' && !['INPUT','TEXTAREA'].includes(document.activeElement.tagName)){event.preventDefault();$('search').focus();} if(event.key === 'Escape' && document.activeElement === $('search')){$('search').value='';renderProjectList();} }); document.addEventListener('visibilitychange',() => { if(!document.hidden) refreshAll(); }); setInterval(() => {if(!document.hidden) refreshAll();},10000);
  renderAll(); refreshAll();
})();
</script>
</body>
</html>`
